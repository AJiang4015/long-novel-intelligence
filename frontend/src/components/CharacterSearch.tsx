// src/components/CharacterSearch.tsx（占位，Task 13 完整实现）
import type { CharacterCandidate } from "../types";

export default function CharacterSearch(_props: { novelId: string; onSelect: (c: CharacterCandidate) => void }) {
  return <p>人物搜索（待实现）</p>;
}
